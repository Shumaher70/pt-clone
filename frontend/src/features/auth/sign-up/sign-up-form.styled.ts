import styled from 'styled-components';
import { Image } from '../../../components/constants';

import * as S from '../sigh-in/sign-in-form.styled';

export const Wrapper = styled(S.Wrapper)``;

export const Logo = styled(Image)``;

export const Title = styled(S.Title)``;

export const Form = styled(S.Form)``;

export const Group = styled(S.Group)``;

export const Label = styled(S.Label)``;

export const Input = styled(S.Input)``;

export const SubmitButton = styled(S.SubmitButton)``;

export const Description = styled(S.Description)``;

export const Register = styled.b``;

export const Error = styled(S.Error)``;
